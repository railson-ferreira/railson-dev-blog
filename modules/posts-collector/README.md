# Posts Collector

```shell
# Configure your AWS IAM or set manually the environment variables
AWS_ACCESS_KEY_ID=<todo>
AWS_REGION=<todo>
AWS_SECRET_ACCESS_KEY=<todo>
# Run the application with posts path as argument
POSTS_PATH=../..
./posts-collector $POSTS_PATH
```